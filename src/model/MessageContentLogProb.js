/**
 * OpenAI Assistants API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import LogProbToken from './LogProbToken';

/**
 * The MessageContentLogProb model module.
 * @module model/MessageContentLogProb
 * @version 0.1.0
 */
class MessageContentLogProb {
    /**
     * Constructs a new <code>MessageContentLogProb</code>.
     * @alias module:model/MessageContentLogProb
     * @param token {String} 
     * @param logprob {Number} 
     * @param bytes {Array.<Number>} 
     * @param topLogprobs {Array.<module:model/LogProbToken>} 
     */
    constructor(token, logprob, bytes, topLogprobs) { 
        
        MessageContentLogProb.initialize(this, token, logprob, bytes, topLogprobs);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, token, logprob, bytes, topLogprobs) { 
        obj['token'] = token;
        obj['logprob'] = logprob;
        obj['bytes'] = bytes;
        obj['top_logprobs'] = topLogprobs;
    }

    /**
     * Constructs a <code>MessageContentLogProb</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MessageContentLogProb} obj Optional instance to populate.
     * @return {module:model/MessageContentLogProb} The populated <code>MessageContentLogProb</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MessageContentLogProb();

            if (data.hasOwnProperty('token')) {
                obj['token'] = ApiClient.convertToType(data['token'], 'String');
            }
            if (data.hasOwnProperty('logprob')) {
                obj['logprob'] = ApiClient.convertToType(data['logprob'], 'Number');
            }
            if (data.hasOwnProperty('bytes')) {
                obj['bytes'] = ApiClient.convertToType(data['bytes'], ['Number']);
            }
            if (data.hasOwnProperty('top_logprobs')) {
                obj['top_logprobs'] = ApiClient.convertToType(data['top_logprobs'], [LogProbToken]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MessageContentLogProb</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MessageContentLogProb</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MessageContentLogProb.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['token'] && !(typeof data['token'] === 'string' || data['token'] instanceof String)) {
            throw new Error("Expected the field `token` to be a primitive type in the JSON string but got " + data['token']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['bytes'])) {
            throw new Error("Expected the field `bytes` to be an array in the JSON data but got " + data['bytes']);
        }
        if (data['top_logprobs']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['top_logprobs'])) {
                throw new Error("Expected the field `top_logprobs` to be an array in the JSON data but got " + data['top_logprobs']);
            }
            // validate the optional field `top_logprobs` (array)
            for (const item of data['top_logprobs']) {
                LogProbToken.validateJSON(item);
            };
        }

        return true;
    }


}

MessageContentLogProb.RequiredProperties = ["token", "logprob", "bytes", "top_logprobs"];

/**
 * @member {String} token
 */
MessageContentLogProb.prototype['token'] = undefined;

/**
 * @member {Number} logprob
 */
MessageContentLogProb.prototype['logprob'] = undefined;

/**
 * @member {Array.<Number>} bytes
 */
MessageContentLogProb.prototype['bytes'] = undefined;

/**
 * @member {Array.<module:model/LogProbToken>} top_logprobs
 */
MessageContentLogProb.prototype['top_logprobs'] = undefined;






export default MessageContentLogProb;

