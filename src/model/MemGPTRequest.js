/**
 * OpenAI Assistants API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import MessageCreate from './MessageCreate';

/**
 * The MemGPTRequest model module.
 * @module model/MemGPTRequest
 * @version 0.1.0
 */
class MemGPTRequest {
    /**
     * Constructs a new <code>MemGPTRequest</code>.
     * @alias module:model/MemGPTRequest
     * @param messages {Array.<module:model/MessageCreate>} The messages to be sent to the agent.
     */
    constructor(messages) { 
        
        MemGPTRequest.initialize(this, messages);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, messages) { 
        obj['messages'] = messages;
    }

    /**
     * Constructs a <code>MemGPTRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MemGPTRequest} obj Optional instance to populate.
     * @return {module:model/MemGPTRequest} The populated <code>MemGPTRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MemGPTRequest();

            if (data.hasOwnProperty('messages')) {
                obj['messages'] = ApiClient.convertToType(data['messages'], [MessageCreate]);
            }
            if (data.hasOwnProperty('run_async')) {
                obj['run_async'] = ApiClient.convertToType(data['run_async'], 'Boolean');
            }
            if (data.hasOwnProperty('stream_steps')) {
                obj['stream_steps'] = ApiClient.convertToType(data['stream_steps'], 'Boolean');
            }
            if (data.hasOwnProperty('stream_tokens')) {
                obj['stream_tokens'] = ApiClient.convertToType(data['stream_tokens'], 'Boolean');
            }
            if (data.hasOwnProperty('return_message_object')) {
                obj['return_message_object'] = ApiClient.convertToType(data['return_message_object'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MemGPTRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MemGPTRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MemGPTRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['messages']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['messages'])) {
                throw new Error("Expected the field `messages` to be an array in the JSON data but got " + data['messages']);
            }
            // validate the optional field `messages` (array)
            for (const item of data['messages']) {
                MessageCreate.validateJSON(item);
            };
        }

        return true;
    }


}

MemGPTRequest.RequiredProperties = ["messages"];

/**
 * The messages to be sent to the agent.
 * @member {Array.<module:model/MessageCreate>} messages
 */
MemGPTRequest.prototype['messages'] = undefined;

/**
 * Whether to asynchronously send the messages to the agent.
 * @member {Boolean} run_async
 * @default false
 */
MemGPTRequest.prototype['run_async'] = false;

/**
 * Flag to determine if the response should be streamed. Set to True for streaming agent steps.
 * @member {Boolean} stream_steps
 * @default false
 */
MemGPTRequest.prototype['stream_steps'] = false;

/**
 * Flag to determine if individual tokens should be streamed. Set to True for token streaming (requires stream_steps = True).
 * @member {Boolean} stream_tokens
 * @default false
 */
MemGPTRequest.prototype['stream_tokens'] = false;

/**
 * Set True to return the raw Message object. Set False to return the Message in the format of the MemGPT API.
 * @member {Boolean} return_message_object
 * @default false
 */
MemGPTRequest.prototype['return_message_object'] = false;






export default MemGPTRequest;

