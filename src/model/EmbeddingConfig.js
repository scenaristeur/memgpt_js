/**
 * OpenAI Assistants API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The EmbeddingConfig model module.
 * @module model/EmbeddingConfig
 * @version 0.1.0
 */
class EmbeddingConfig {
    /**
     * Constructs a new <code>EmbeddingConfig</code>.
     * Embedding model configuration
     * @alias module:model/EmbeddingConfig
     * @param embeddingEndpointType {String} The endpoint type for the model.
     * @param embeddingModel {String} The model for the embedding.
     * @param embeddingDim {Number} The dimension of the embedding.
     */
    constructor(embeddingEndpointType, embeddingModel, embeddingDim) { 
        
        EmbeddingConfig.initialize(this, embeddingEndpointType, embeddingModel, embeddingDim);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, embeddingEndpointType, embeddingModel, embeddingDim) { 
        obj['embedding_endpoint_type'] = embeddingEndpointType;
        obj['embedding_model'] = embeddingModel;
        obj['embedding_dim'] = embeddingDim;
    }

    /**
     * Constructs a <code>EmbeddingConfig</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EmbeddingConfig} obj Optional instance to populate.
     * @return {module:model/EmbeddingConfig} The populated <code>EmbeddingConfig</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EmbeddingConfig();

            if (data.hasOwnProperty('embedding_endpoint_type')) {
                obj['embedding_endpoint_type'] = ApiClient.convertToType(data['embedding_endpoint_type'], 'String');
            }
            if (data.hasOwnProperty('embedding_endpoint')) {
                obj['embedding_endpoint'] = ApiClient.convertToType(data['embedding_endpoint'], 'String');
            }
            if (data.hasOwnProperty('embedding_model')) {
                obj['embedding_model'] = ApiClient.convertToType(data['embedding_model'], 'String');
            }
            if (data.hasOwnProperty('embedding_dim')) {
                obj['embedding_dim'] = ApiClient.convertToType(data['embedding_dim'], 'Number');
            }
            if (data.hasOwnProperty('embedding_chunk_size')) {
                obj['embedding_chunk_size'] = ApiClient.convertToType(data['embedding_chunk_size'], 'Number');
            }
            if (data.hasOwnProperty('azure_endpoint')) {
                obj['azure_endpoint'] = ApiClient.convertToType(data['azure_endpoint'], 'String');
            }
            if (data.hasOwnProperty('azure_version')) {
                obj['azure_version'] = ApiClient.convertToType(data['azure_version'], 'String');
            }
            if (data.hasOwnProperty('azure_deployment')) {
                obj['azure_deployment'] = ApiClient.convertToType(data['azure_deployment'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EmbeddingConfig</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EmbeddingConfig</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EmbeddingConfig.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['embedding_endpoint_type'] && !(typeof data['embedding_endpoint_type'] === 'string' || data['embedding_endpoint_type'] instanceof String)) {
            throw new Error("Expected the field `embedding_endpoint_type` to be a primitive type in the JSON string but got " + data['embedding_endpoint_type']);
        }
        // ensure the json data is a string
        if (data['embedding_endpoint'] && !(typeof data['embedding_endpoint'] === 'string' || data['embedding_endpoint'] instanceof String)) {
            throw new Error("Expected the field `embedding_endpoint` to be a primitive type in the JSON string but got " + data['embedding_endpoint']);
        }
        // ensure the json data is a string
        if (data['embedding_model'] && !(typeof data['embedding_model'] === 'string' || data['embedding_model'] instanceof String)) {
            throw new Error("Expected the field `embedding_model` to be a primitive type in the JSON string but got " + data['embedding_model']);
        }
        // ensure the json data is a string
        if (data['azure_endpoint'] && !(typeof data['azure_endpoint'] === 'string' || data['azure_endpoint'] instanceof String)) {
            throw new Error("Expected the field `azure_endpoint` to be a primitive type in the JSON string but got " + data['azure_endpoint']);
        }
        // ensure the json data is a string
        if (data['azure_version'] && !(typeof data['azure_version'] === 'string' || data['azure_version'] instanceof String)) {
            throw new Error("Expected the field `azure_version` to be a primitive type in the JSON string but got " + data['azure_version']);
        }
        // ensure the json data is a string
        if (data['azure_deployment'] && !(typeof data['azure_deployment'] === 'string' || data['azure_deployment'] instanceof String)) {
            throw new Error("Expected the field `azure_deployment` to be a primitive type in the JSON string but got " + data['azure_deployment']);
        }

        return true;
    }


}

EmbeddingConfig.RequiredProperties = ["embedding_endpoint_type", "embedding_model", "embedding_dim"];

/**
 * The endpoint type for the model.
 * @member {String} embedding_endpoint_type
 */
EmbeddingConfig.prototype['embedding_endpoint_type'] = undefined;

/**
 * @member {String} embedding_endpoint
 */
EmbeddingConfig.prototype['embedding_endpoint'] = undefined;

/**
 * The model for the embedding.
 * @member {String} embedding_model
 */
EmbeddingConfig.prototype['embedding_model'] = undefined;

/**
 * The dimension of the embedding.
 * @member {Number} embedding_dim
 */
EmbeddingConfig.prototype['embedding_dim'] = undefined;

/**
 * @member {Number} embedding_chunk_size
 */
EmbeddingConfig.prototype['embedding_chunk_size'] = undefined;

/**
 * @member {String} azure_endpoint
 */
EmbeddingConfig.prototype['azure_endpoint'] = undefined;

/**
 * @member {String} azure_version
 */
EmbeddingConfig.prototype['azure_version'] = undefined;

/**
 * @member {String} azure_deployment
 */
EmbeddingConfig.prototype['azure_deployment'] = undefined;






export default EmbeddingConfig;

